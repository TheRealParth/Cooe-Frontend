import {Component, Input, OnInit} from '@angular/core';

@Component({
  selector: 'user-image',
  //TODO: Make the template
  template: ``
})

export class UserImage implements OnInit {
  //TODO: add two inputs (ImageURL and default Image)
  constructor(){

  }
  //TODO: return the location of the appropriate image based on input (if no input load default)
  getImage(){

  }
  
  ngOnInit(){
    
  }
}
